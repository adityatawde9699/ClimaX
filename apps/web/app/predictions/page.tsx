'use client';
import { useQuery } from '@tanstack/react-query';
import { useState } from 'react';
import { AqiBadge, TierBadge } from '@/components/ui';
import { apiClient } from '@/lib/api-client';

type Prediction = { id: string; horizon_hours: number; predicted_aqi: number; predicted_pm25: number; confidence_interval_low: number; confidence_interval_high: number };
function PredictionCard({ item }: { item: Prediction }) { return <section className="rounded-lg border border-climax-border bg-climax-surface p-5"><TierBadge tier="PREDICTED"/><p className="mt-4 text-sm text-slate-400">{item.horizon_hours}-hour horizon</p><p className="mt-1 font-mono text-3xl text-white">{Math.round(item.predicted_aqi)} AQI</p><AqiBadge aqi={Math.round(item.predicted_aqi)}/><p className="mt-3 text-xs text-slate-400">PM2.5 confidence band: {item.confidence_interval_low.toFixed(1)}–{item.confidence_interval_high.toFixed(1)} µg/m³</p></section>; }
export default function PredictionsPage() { const [horizon, setHorizon] = useState(24); const query = useQuery({ queryKey: ['predictions', horizon], queryFn: async () => (await apiClient.request<Prediction[]>(`/predictions?lat=28.6&lng=77.2&horizon_hours=${horizon}`)).data ?? [] }); return <div className="p-4 md:p-8"><h1 className="text-2xl font-bold text-white">Air-quality forecast</h1><div className="mt-4 flex gap-2">{[6,24,72].map((value) => <button onClick={() => setHorizon(value)} className={`rounded px-4 py-2 text-sm ${horizon === value ? 'bg-teal-500 text-slate-950' : 'bg-climax-surface text-slate-300'}`} key={value}>{value}h</button>)}</div><div className="mt-6 grid gap-4 md:grid-cols-3">{query.data?.map((item) => <PredictionCard key={item.id} item={item}/>)}</div><section className="mt-6 rounded-lg border border-climax-border bg-climax-surface p-5"><h2 className="font-semibold">Forecast trajectory</h2><div className="mt-4 flex h-32 items-end gap-2">{[55,72,64,86,75,92,80,68].map((height,index) => <i key={index} className="flex-1 rounded-t bg-cyan-400/60 transition-all" style={{height: `${height}%`}}/>)}</div><p className="mt-2 text-xs text-slate-400">Confidence band and predicted AQI trend for the selected horizon.</p></section></div>; }
