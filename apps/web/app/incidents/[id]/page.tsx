import Link from 'next/link';
import { SeverityIndicator, StatusBadge, TierBadge } from '@/components/ui';
export default function IncidentPage({ params }: { params: { id: string } }) { const steps = ['OPEN', 'INVESTIGATING', 'DISPATCHED', 'MITIGATED', 'RESOLVED']; return <div className="mx-auto max-w-4xl p-4 md:p-8"><Link className="text-sm text-teal-300" href="/dashboard">← Dashboard</Link><div className="mt-4 flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm text-slate-400">Incident {params.id}</p><h1 className="text-2xl font-bold text-white">Environmental incident detail</h1></div><div className="flex gap-2"><SeverityIndicator severity="HIGH"/><StatusBadge status="INVESTIGATING"/></div></div><section className="mt-6 rounded-lg border border-climax-border bg-climax-surface p-5"><TierBadge tier="OBSERVED"/><h2 className="mt-3 font-semibold text-white">Lifecycle timeline</h2><ol className="mt-5 space-y-4">{steps.map((step, index) => <li key={step} className="flex items-center gap-3"><span className={`flex h-7 w-7 items-center justify-center rounded-full text-xs ${index < 2 ? 'bg-teal-500 text-slate-950' : 'bg-slate-800 text-slate-400'}`}>{index + 1}</span><span className={index < 2 ? 'text-white' : 'text-slate-500'}>{step.replace('_', ' ')}</span></li>)}</ol></section></div>; }
