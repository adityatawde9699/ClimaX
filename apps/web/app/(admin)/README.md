# Route Group: (admin)

System administration, RBAC, sensor mesh telemetry, and pipeline health.

## Sub-routes:
- `/admin/users` - Authority & researcher credential management
- `/admin/sensors` - Sensor calibration, heartbeat monitoring, and registry
- `/admin/pipelines` - Ingestion queue status, BigQuery streaming health
- `/admin/models` - AI inference latency, drift metrics, and token cost ledger
